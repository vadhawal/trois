{% load mezzanine_tags i18n future %}
<link rel="stylesheet" href="{{ STATIC_URL }}css/customnavbar.css">
<form action="{% url "search" %}" class="span8 searchbox" >

<!--<div class='span8' style='height: inherit !important;'>
    <input class='span7 input-large' placeholder="{% trans "Search Items" %}" type="text" name="q" value="{{ request.REQUEST.q }}"/>
    <button btn add-on'>
        <i class="icon-search"></i>
    </button>
</div>
-->

        <input id="search" class="span7" type="text" style="height:45px;" placeholder="{% trans "Search Items" %}" name="q" value="{{ request.REQUEST.q }}">
<button id="submit" class="span1" type="submit"><i class="icon-search"></i></button>
  

<!-- <input class="span6 search-query" placeholder="{% trans "Search Items" %}" type="text" name="q" value="{{ request.REQUEST.q }}">

{% if search_model_choices %}
    {% if search_model_choices|length == 1 %}
    <input type="hidden" name="type" value="{{ search_model_choices.0.1 }}">
    {% else %}
    <select name="type" style="height:35px;">
        <option value="">{% trans "Everything" %}</option>
        {% for verbose_name, model in search_model_choices %}
        <option value="{{ model }}"
            {% if model == request.REQUEST.type  %}selected{% endif %}>
            {{ verbose_name }}
        </option>
        {% endfor %}
        <input type="submit" class="btn btn-primary" value="{% trans "Go" %}" style="height:35px;">
    </select>
    {% endif %}
{% endif %}
-->
</form>
